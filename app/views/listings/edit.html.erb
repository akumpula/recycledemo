<h1>Editing Listing</h1>

<%= render 'form' %>

<%= link_to 'Show', @listing, class: "btn btn-link" %> |
<%= link_to 'Back', listings_path, class: "btn btn-link" %>

<%= form_for @listing, :url => listings_path, :html => { :multipart => true } do |form| %>
<% end %>
